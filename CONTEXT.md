# Data Agent 微调上下文

本上下文定义问数系统微调实验中使用的核心业务语言，确保训练数据、评估结果和后续实现使用一致的概念。

## 微调实验

**阶段一 SQL 生成任务**:
仅让模型根据完整召回上下文生成一条纯 SQL；不把召回、关键词提取、澄清或纠错能力混入本阶段目标。
_Avoid_: 端到端 Agent 微调

**本地训练环境**:
用于训练候选 SQL 生成模型的受控 GPU 环境；当前实验优先使用本机 RTX 5070 Laptop GPU，显存不足时再迁移到已有 GPU 服务器。
_Avoid_: 生产推理环境

**阶段一底座模型**:
首轮候选模型为 `Qwen/Qwen2.5-Coder-3B-Instruct`，采用 4-bit QLoRA 在本地 GPU 上验证完整训练链路。
_Avoid_: 直接在 8GB 显存上默认训练 7B 以上模型

**微调目标**:
让模型根据问数问题和已确认的召回上下文生成语义正确、可执行且只读的 SQL。
_Avoid_: 直接把整个 Agent 流程视为一个不可分割的训练目标

**召回上下文**:
为回答一个问数问题而提供给 SQL 生成模型的表、字段、字段值、指标定义、计算规则、时间信息和数据库信息。
_Avoid_: 知识库、提示词上下文（后者只描述承载形式）

**SQL 生成样本**:
由用户问数问题、召回上下文和人工确认的标准 SQL 组成的一条训练或评估记录。
_Avoid_: 问题-SQL 对（缺少生成 SQL 所需的上下文）

## 评估语言

**结果正确率**:
SQL 在目标数据集上返回的结果与参考答案在业务语义上等价的比例。
_Avoid_: SQL 字符串准确率、文本匹配率

**可执行率**:
生成的 SQL 能在目标数据库方言和权限约束下成功执行的比例。
_Avoid_: 语法通过率（仅语法正确不足以代表可执行）

**只读安全通过率**:
生成的 SQL 同时满足只读要求、未执行写入或破坏性操作、且未越权访问的比例。
_Avoid_: SQL 安全率（范围过于模糊）

## 数据与实验边界

**纯 SQL 输出契约**:
第一阶段 SQL 生成模型的响应只包含一条可执行的查询语句，不包含解释、Markdown 标记或澄清文本。
_Avoid_: 混合式回答

**高质量正样本**:
召回上下文完整、SQL 经人工确认或业务验证、并满足目标数据库和只读约束的 SQL 生成样本。
_Avoid_: 模型自举样本、仅语法通过样本

**种子数据集**:
从当前零售主题 MySQL 快照构建的首批高质量正样本集合，目标规模为 120 条，覆盖训练、验证和测试的业务隔离切分。
_Avoid_: 直接从运行日志复制的训练集

**权威种子来源**:
首批数据只来自仓库内 `docker_windows/mysql/dw.sql` 的零售快照和人工编写的问题，不纳入生产日志或未经脱敏的历史数据。
_Avoid_: 生产查询日志训练集

**首批业务指标**:
订单数、销售金额、销售数量、平均订单金额，以及按地区、客户等级、商品类别、品牌和时间维度进行组合分析的明确口径。
_Avoid_: 快照中没有定义的复杂指标

**查询家族**:
用于业务隔离切分和覆盖率统计的 SQL 问题类别，包括筛选、聚合、时间分析、维度分组、排序/Top N、多表关联、指标组合和安全/方言边界。
_Avoid_: 随机样本标签

**候选样本**:
由 schema、业务模板或自动化流程提出、尚未完成业务人工确认的问数与 SQL 记录；候选样本不能进入正式训练集。
_Avoid_: 标准 SQL

**业务隔离切分**:
按照业务主题、查询模板或时间边界划分训练集、验证集和测试集，使同一问题变体不跨集合重复出现。
_Avoid_: 逐条随机切分

**结果集等价**:
候选 SQL 与参考 SQL 在隔离 MySQL 8 快照执行后，经过空值、数值精度和无序行规范化仍返回同一业务结果。
_Avoid_: SQL 文本相等

**只读三层保护**:
由 SQL 静态检查、单语句限制和数据库只读账号共同构成的评估安全边界。
_Avoid_: 仅依赖提示词保证安全

**标准 SQL确认人**:
对候选 SQL 的业务口径、字段选择和结果含义承担最终确认责任的人；本轮由 Codex 代理依据快照 schema、静态规则和 SQL 语义执行代理审核，尚未替代业务负责人签字。
_Avoid_: 模型自动确认、未经披露的业务签字

**审核记录**:
与候选样本关联的审核状态、确认人、拒绝原因和数据库执行结果；未审核或被拒绝的候选不能进入正式切分。
_Avoid_: 只保留最终 SQL 的无来源数据

**上线门槛**:
微调模型相对当前 API 模型基线的结果正确率至少提升约 5 个百分点，同时可执行率不下降、只读安全通过率保持 100%。
_Avoid_: 只看训练损失、只看 SQL 字符串相似度

**模型基线**:
当前 API 模型作为对照系统；微调实验使用可控环境中可训练、可部署的中文代码/指令模型。
_Avoid_: 直接假设 API 模型权重可获得或可微调

**对话式 SQL 样本**:
以固定系统指令、用户问题和召回上下文作为输入，以一条纯 SQL 作为唯一目标输出的 JSONL 记录。
_Avoid_: 把完整运行日志直接作为训练样本

**隔离评估数据库**:
用于执行候选 SQL 并比对参考结果的 MySQL 8 数据库快照，与生产数据和写权限隔离。
_Avoid_: 直接在生产库评估

**模型切换端点**:
提供统一 HTTP/兼容协议的推理入口，使应用可以在 API 基线和微调模型之间切换，而无需改变问数图的业务节点。
_Avoid_: 在 LangGraph 节点中硬编码具体模型服务

**训练迁移条件**:
当本机 GPU 无法稳定完成 3B QLoRA 的量化加载或训练时，将同一底座和数据流水线迁移到已有 GPU 服务器，不因环境失败擅自更换底座型号。
_Avoid_: 为绕过显存问题随意换模型
